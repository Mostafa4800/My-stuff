function setup() {
    var canvas = createCanvas(430, 932);
    canvas.parent('canvas'); //'canvas' is the id of the target HTML element
}

function draw() {
  background(220);
  // Draw a circle
ellipse(50, 50, 80, 80);
}
